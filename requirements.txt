# today-house-price — 런타임 의존성 (pip 사용자용 · 레거시)
#
# [비활성] 본 프로젝트는 uv(pyproject.toml + uv.lock)를 정본으로 사용합니다.
# pip 설치가 필요하면 아래 export로 파일을 재생성한 뒤 주석을 해제하세요.
#
#   uv export --no-dev --no-emit-project --no-hashes -o requirements.txt
#
# Python >=3.11
# 직접 의존성: requests (서울 열린데이터광장 API)
#
# certifi==2026.5.20
#     # via requests
# charset-normalizer==3.4.7
#     # via requests
# idna==3.18
#     # via requests
# requests==2.34.2
# urllib3==2.7.0
#     # via requests
