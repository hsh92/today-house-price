# today-house-price — 개발 의존성 (pip 사용자용 · 레거시)
#
# [비활성] uv sync 로 개발 환경을 구성하세요.
# pip 설치가 필요하면 export 후 주석을 해제하세요.
#
#   uv export --only-dev --no-emit-project --no-hashes -o requirements-dev.txt
#   (상단 -r requirements.txt 는 수동 유지)
#
# 직접 dev 의존성: pytest, pytest-cov, ruff
#
# -r requirements.txt
#
# colorama==0.4.6 ; sys_platform == 'win32'
#     # via pytest
# coverage==7.14.1
#     # via pytest-cov
# iniconfig==2.3.0
#     # via pytest
# packaging==26.2
#     # via pytest
# pluggy==1.6.0
#     # via
#     #   pytest
#     #   pytest-cov
# pygments==2.20.0
#     # via pytest
# pytest==9.1.0
#     # via pytest-cov
# pytest-cov==7.1.0
# ruff==0.15.17
# tomli==2.4.1 ; python_full_version <= '3.11'
#     # via coverage
