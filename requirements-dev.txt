# today-house-price — 개발 의존성 (pip 사용자용)
#
# 설치: pip install -r requirements-dev.txt
# (런타임 requirements.txt 포함)
#
# pyproject.toml [dependency-groups] dev 와 동기화:
#   uv export --only-dev --no-emit-project --no-hashes -o requirements-dev.txt
#   (수동으로 상단 -r requirements.txt 유지)
#
# 직접 dev 의존성: pytest, pytest-cov, ruff

-r requirements.txt

colorama==0.4.6 ; sys_platform == 'win32'
    # via pytest
coverage==7.14.1
    # via pytest-cov
iniconfig==2.3.0
    # via pytest
packaging==26.2
    # via pytest
pluggy==1.6.0
    # via
    #   pytest
    #   pytest-cov
pygments==2.20.0
    # via pytest
pytest==9.1.0
    # via pytest-cov
pytest-cov==7.1.0
ruff==0.15.17
tomli==2.4.1 ; python_full_version <= '3.11'
    # via coverage
